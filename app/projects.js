(function(){
  const projects=window.DEVOPS_PROJECTS||[], $=id=>document.getElementById(id), esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const search=$('projectSearch'),grid=$('projectGrid'),detail=$('projectDetail');
  function renderGrid(){const q=(search.value||'').toLowerCase().trim();const list=projects.filter(p=>!q||[p.title,p.level,p.problem,...p.architecture,...p.tools.flat()].join(' ').toLowerCase().includes(q));grid.innerHTML=list.map(p=>`<article class="panel lab-card"><div class="lab-top"><span class="lab-level">${esc(p.level)}</span><span>${esc(p.architecture.slice(0,3).join(' → '))}</span></div><h3>${esc(p.title)}</h3><p>${esc(p.problem)}</p><a class="tool-link" href="projects.html?id=${encodeURIComponent(p.id)}#detail">Open full project <i class="bi bi-arrow-right"></i></a></article>`).join('')||'<div class="module-empty">No project found.</div>'}
  function renderDetail(){const id=new URLSearchParams(location.search).get('id')||projects[0]?.id,p=projects.find(x=>x.id===id)||projects[0];if(!p)return;detail.innerHTML=`<div class="project-heading"><div><p class="panel-kicker">${esc(p.level)} project</p><h2>${esc(p.title)}</h2><p><strong>Real problem:</strong> ${esc(p.problem)}</p></div><span class="production-badge">WHY + HOW</span></div><h3>Architecture</h3><div class="project-architecture">${p.architecture.map((x,i,a)=>`<span>${esc(x)}</span>${i<a.length-1?'<i class="bi bi-arrow-right"></i>':''}`).join('')}</div><h3>Why each tool / component exists</h3><div class="why-grid">${p.tools.map(x=>`<div><strong>${esc(x[0])}</strong><p>${esc(x[1])}</p></div>`).join('')}</div><div class="what-happens"><strong>What is happening internally?</strong><p>A reviewed change enters the architecture from the left, each component performs one responsibility, hands validated state or data to the next component, and the final production outcome is verified through health checks and observability instead of assumption.</p></div><h3>Step-by-step build</h3><ol>${p.steps.map(x=>`<li>${esc(x)}</li>`).join('')}</ol><div class="production-grid"><article class="panel"><h3>Verify</h3><ul>${p.verify.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article><article class="panel"><h3>What can fail?</h3><ul>${p.failures.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article><article class="panel"><h3>Security</h3><ul>${p.security.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></article></div><h3>Production improvements</h3><div class="connection-row">${p.production.map(x=>`<span>${esc(x)}</span>`).join('')}</div>`}
  search.addEventListener('input',renderGrid);renderGrid();renderDetail();
  const theme=$('projectTheme');function sync(){const d=document.documentElement.dataset.theme==='dark';theme.innerHTML=`<i class="bi ${d?'bi-sun':'bi-moon-stars'}"></i><span>${d?'Light theme':'Dark theme'}</span>`}theme.addEventListener('click',()=>{const n=document.documentElement.dataset.theme==='dark'?'light':'dark';document.documentElement.dataset.theme=n;try{localStorage.setItem('linuxStudyTheme',n)}catch(e){}sync()});sync();
  const s=$('sidebar'),o=$('sidebarOverlay'),m=$('mobileMenuButton');function close(){s?.classList.remove('open');document.body.classList.remove('sidebar-open');m?.setAttribute('aria-expanded','false')}m?.addEventListener('click',()=>{const x=s?.classList.toggle('open');document.body.classList.toggle('sidebar-open',!!x);m.setAttribute('aria-expanded',x?'true':'false')});o?.addEventListener('click',close);document.querySelectorAll('.sidebar a').forEach(a=>a.addEventListener('click',close));
})();